/**
 * Modelo de datos compartido: representaciones de grafos (lista de
 * adyacencia, lista de aristas), la grilla de la Mision 1 y los objetos de
 * resultado que cada algoritmo devuelve (distancias, caminos, matriz N x N,
 * aristas del MST).
 *
 * Estos tipos son el contrato entre el nucleo algoritmico, los orquestadores
 * de mision y la GUI: ninguna de esas capas se habla directamente, solo a
 * traves de este paquete.
 */
package com.eia.felinas.modelo;
