/**
 * Lectura tolerante de la entrada (requisito 2.2 del enunciado): el texto se
 * procesa como un flujo de tokens separados por cualquier espacio en blanco,
 * sin asumir un numero fijo de tokens por linea y tolerando lineas vacias y
 * espacios finales. La entrada malformada produce errores legibles, nunca un
 * stack trace.
 */
package com.eia.felinas.parser;
